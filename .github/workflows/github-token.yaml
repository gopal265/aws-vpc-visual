name: Github Token

on:
  push:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Read repostiroy
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run:  gh repo view ${{ github.repository }}
