name : My Second Flow

on:
  workflow_dispatch:

jobs:
  app:
    runs-on: ubuntu-latest

    steps:
      - name: Check Directory 
        run: |
             pwd
             ls -a

      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name : Check Directory again
        run: |
            pwd
            ls -a
      