name : My Second Flow

on:
  push:

jobs:
  app:
    runs-on: ubuntu-latest

    steps:
      - name: Check Directory 
        run: |
             pwd
             ls -a

      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name : Check Directory again
        run: |
            pwd
            ls -a
      