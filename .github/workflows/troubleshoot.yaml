name: Trouble Shooting

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Broken Command
        run: |
          echo "Starting"
          non-exist-command
          echo "Finished"