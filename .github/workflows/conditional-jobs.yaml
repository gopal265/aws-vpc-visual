name: Conditional Flow

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
    
  notify:
    runs-on: ubuntu-latest
    needs: test
    if: failure()
    steps:
      - run: echo "Test cases failed , sending notification"
